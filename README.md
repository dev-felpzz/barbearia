# Barbearia SaaS

## Estrutura
- `templates/` — views Jinja2
- `static/css` — estilos
- `static/js` — scripts
- `static/img` — imagens
- `assets/` — ícones e fontes

## Setup
```
pip install flask
flask run
```
